using System;
using NewEngine.Engine.Core;

namespace NewEngine.Engine.Rendering.MeshLoading.FBX {
    public class FbxModel {
        //private int _major;
        //private int _minor;

        //private int _revision;

        public FbxModel(string filename) {
            //var manager = FBXManager.Create();
            //var settings = FBXIOSettings.Create(manager, "IOSRoot");

            //manager.SetIOSettings(settings);
            //manager.LoadPluginsDirectory(Environment.CurrentDirectory, "");

            //var scene = FBXScene.Create(manager, filename);
            //var importer = FBXImporter.Create(manager, "");

            //var initializeResult = importer.Initialize(filename, -1, manager.GetIOSettings());

            //FBXManager.GetFileFormatVersion(ref _major, ref _minor, ref _revision);

            //var importResult = importer.Import(scene);
            //if (importResult == false)
            //    LogManager.Debug("ERROR!");
        }
    }
}